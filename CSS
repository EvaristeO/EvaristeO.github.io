<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS - Evariste's Website</title>
    <style>
        body {
            font-family: Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #7af07a;
            text-align: center;
        }

        header {
            background: rgb(167, 205, 167);
            padding: 20px;
            border-bottom: 2px solid black;
        }

        h1 {
            font-size: 48px;
            margin: 0;
        }

        main {
            max-width: 600px;
            margin: 20px auto;
            background: white;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: left;
        }

        nav ul {
            list-style: none;
            padding: 0;
        }

        nav ul li {
            display: inline;
            margin: 0 15px;
        }

        nav ul li a {
            text-decoration: none;
            color: black;
            font-weight: bold;
        }

        footer {
            margin-top: 20px;
            padding: 10px;
            background: black;
            color: white;
        }
    </style>
</head>
<body>
    <header>
        <h1>CSS</h1>
    </header>

    <nav>
        <ul>
            <li><a href="index.html">Home</a></li>
        </ul>
    </nav>

    <main>
        <h2>What is CSS</h2>
        <p>CSS (Cascading Style Sheets) is a style sheet language used to describe the presentation of a document written in HTML or XML. CSS controls how elements look — such as colors, fonts, layouts, and spacing — without affecting the actual content structure.</p>
        <p>By separating content (HTML) from presentation (CSS), developers can create visually appealing and consistent designs across
